---
title: Introduction
description: Stream any movie or TV show via TMDB ID — one API, seven providers, zero config.
---

<img src="/images/rounded-banner.png" alt="Vyla API" />

## What is Vyla API?

Vyla API is a **Cloudflare Pages API** that scrapes streaming sources from 7 providers concurrently and serves them through a unified JSON interface. Pass any TMDB ID and get back every working source, already verified and proxied.

<CardGroup cols={2}>
  <Card title="Stream URLs">
    All working HLS sources from every provider — verified live before being returned
  </Card>
  <Card title="CORS Proxy">
    Every URL is routed through Cloudflare with full m3u8 segment rewriting — no CORS issues in any browser player
  </Card>
  <Card title="Health Check">
    Per-provider health endpoint shows which sources are up and their response times
  </Card>
  <Card title="Single Provider Testing">
    Test any individual provider by key to debug or compare results
  </Card>
</CardGroup>

---

## Base URL

```
https://vyla-api.pages.dev
```

All endpoints return `Access-Control-Allow-Origin: *` — no credentials needed.

---

## Quick Start

<CodeGroup>
```bash Movie Sources
curl https://vyla-api.pages.dev/api/movie?id=27205
```

```bash TV Episode Sources
curl "https://vyla-api.pages.dev/api/tv?id=1396&season=1&episode=1"
```

```bash Health Check
curl https://vyla-api.pages.dev/api/health
```
</CodeGroup>

<Tip>
  **New here?** Jump to the [Quickstart](/api-reference/quickstart) for a guided walkthrough.
</Tip>

---

## Endpoint Overview

| Endpoint | Description |
|----------|-------------|
| `GET /api/movie` | All working sources for a movie |
| `GET /api/tv` | All working sources for a TV episode |
| `GET /api/health` | Per-provider health check |
| `GET /api/test/<id>?source=<key>` | Test a single provider |

---

## Providers

Sources are fetched from all 7 providers concurrently. Each result is verified with a live stream check — only working sources are returned.

| Key | Label |
|-----|-------|
| `vixsrc` | vixsrc |
| `icefy` | Icefy |
| `vidzee` | VidZee |
| `vidnest` | VidNest |
| `vidsrc` | VidSrc |
| `vidrock` | VidRock |
| `videasy` | Videasy |

---

## Source Object Reference

```json
{
  "source": "vixsrc",
  "label": "vixsrc",
  "url": "/api?url=<encoded_stream_url>&vl=1"
}
```

<ResponseField name="source" type="string">
  Internal provider key.
</ResponseField>

<ResponseField name="label" type="string">
  Human-readable provider name.
</ResponseField>

<ResponseField name="url" type="string">
  Proxied stream URL — routed through `/api` with CORS and m3u8 rewriting applied. Pass directly to HLS.js or any player.
</ResponseField>

---

## CORS Headers

Every endpoint returns:

```
Access-Control-Allow-Origin: *
Access-Control-Allow-Methods: GET, HEAD, OPTIONS
Access-Control-Allow-Headers: *
```