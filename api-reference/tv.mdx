---
title: TV Show
description: Scrape streaming sources for a TV episode by TMDB ID
---

<Note>
  For enriched output with pre-built `download_url`, `ffmpeg_command`, and deeper deduplication across all providers in parallel, use [`/api/stream/tv`](/api-reference/stream-tv) instead.
</Note>

## Endpoint

```
GET /api/tv
```

Scrapes all providers for a TV episode and returns the raw source list. Sources are sorted by quality and filtered to English audio only. No deduplication beyond exact URL matches is applied at this layer.

---

## Parameters

<ParamField query="id" type="string" required>
  TMDB **series** ID — not episode ID. Find it at [themoviedb.org](https://www.themoviedb.org), e.g. `themoviedb.org/tv/456` → `456`.
</ParamField>

<ParamField query="season" type="string" default="1">
  Season number.
</ParamField>

<ParamField query="episode" type="string" default="1">
  Episode number.
</ParamField>

---

## Example Requests

<CodeGroup>
```bash Season 1, Episode 1 (defaults)
curl https://vyla-api.pages.dev/api/tv?id=456
```

```bash Specific episode
curl "https://vyla-api.pages.dev/api/tv?id=456&season=2&episode=3"
```

```js JavaScript
const res = await fetch(
  "https://vyla-api.pages.dev/api/tv?id=456&season=2&episode=3"
);
const data = await res.json();
```

```python Python
import requests

data = requests.get(
  "https://vyla-api.pages.dev/api/tv",
  params={"id": 456, "season": 2, "episode": 3}
).json()
```
</CodeGroup>

---

## Response

<Tabs>
  <Tab title="Success">
    ```json
    {
      "success": true,
      "results_found": 9,
      "sources": [
        {
          "url": "https://vixsrc.to/playlist/202976?token=...&expires=...&h=1",
          "type": "hls",
          "quality": "1080p",
          "provider": "VixSrc",
          "audioTracks": [
            { "language": "eng", "label": "English" }
          ],
          "headers": {
            "Referer": "https://vixsrc.to",
            "Origin": "https://vixsrc.to"
          },
          "vlc_url": "https://vixsrc.to/playlist/202976?token=...&expires=...&h=1"
        },
        {
          "url": "https://bcdnxw.hakunaymatata.com/bt/abc123.mp4?sign=...&t=...",
          "type": "mp4",
          "quality": "360p",
          "provider": "VidRock",
          "audioTracks": [
            { "language": "eng", "label": "English" }
          ],
          "headers": {
            "Referer": "https://lok-lok.cc/",
            "Origin": "https://lok-lok.cc/"
          },
          "vlc_url": "https://bcdnxw.hakunaymatata.com/bt/abc123.mp4?sign=...&t=..."
        }
      ],
      "subtitles": [
        {
          "url": "https://vixsrc.to/playlist/202976?type=subtitle&rendition=3-eng&token=...&expires=...&edge=...",
          "label": "English [CC]",
          "format": "vtt"
        },
        {
          "url": "https://vdrk.b-cdn.net/cache/tv/456/1/1/English.vtt",
          "label": "English",
          "format": "vtt"
        }
      ]
    }
    ```
  </Tab>
  <Tab title="Error">
    ```json
    {
      "success": false,
      "error": "Missing id"
    }
    ```
  </Tab>
</Tabs>

---

## Source Object Fields

<ResponseField name="url" type="string">
  Direct stream URL.
</ResponseField>

<ResponseField name="type" type="string">
  `hls`, `mp4`, or `mkv`
</ResponseField>

<ResponseField name="quality" type="string">
  e.g. `1080p`, `720p`, `HD`, `unknown`
</ResponseField>

<ResponseField name="provider" type="string">
  Which provider this source came from.
</ResponseField>

<ResponseField name="audioTracks" type="array">
  Language and label of available audio tracks.
</ResponseField>

<ResponseField name="headers" type="object">
  Required headers to forward when fetching this URL directly.
</ResponseField>

<ResponseField name="vlc_url" type="string">
  Same as `url` on this endpoint — not proxied.
</ResponseField>

---

<Warning>
  The `headers` field on each source **must be forwarded** when fetching the stream directly. Use `/api/stream/tv` to get pre-built `vlc_url` (proxied), `download_url`, and `ffmpeg_command` with headers already encoded.
</Warning>

<Note>
  The `id` parameter refers to the **series**, not an individual episode. Season and episode numbers are passed as separate `season` and `episode` parameters.
</Note>