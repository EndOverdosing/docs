---
title: TV Show
description: Scrape streaming sources for a TV episode by TMDB ID
---

## Endpoint

```
GET /api/tv
```

## Parameters

| Name      | Type   | Required | Default | Description       |
|-----------|--------|----------|---------|-------------------|
| `id`      | string | ✅       | —       | TMDB series ID    |
| `season`  | string | ❌       | `1`     | Season number     |
| `episode` | string | ❌       | `1`     | Episode number    |

## Example Requests

```bash
# Season 1, Episode 1 (defaults)
curl https://vyla-api.pages.dev/api/tv?id=456

# Specific episode
curl https://vyla-api.pages.dev/api/tv?id=456&season=2&episode=3
```

## Example Response

```json
{
  "success": true,
  "results_found": 9,
  "sources": [
    {
      "url": "https://vixsrc.to/playlist/202976?token=...&expires=...&h=1",
      "type": "hls",
      "quality": "1080p",
      "provider": "VixSrc",
      "audioTracks": [
        { "language": "eng", "label": "English" }
      ],
      "headers": {
        "Referer": "https://vixsrc.to",
        "Origin": "https://vixsrc.to"
      }
    },
    {
      "url": "https://bcdnxw.hakunaymatata.com/bt/abc123.mp4?sign=...&t=...",
      "type": "mp4",
      "quality": "360p",
      "provider": "VidRock",
      "audioTracks": [
        { "language": "eng", "label": "English" }
      ],
      "headers": {
        "Referer": "https://lok-lok.cc/",
        "Origin": "https://lok-lok.cc/"
      }
    }
  ],
  "subtitles": [
    {
      "url": "https://vixsrc.to/playlist/202976?type=subtitle&rendition=3-eng&token=...&expires=...&edge=...",
      "label": "English [CC]",
      "format": "vtt"
    },
    {
      "url": "https://vdrk.b-cdn.net/cache/tv/456/1/1/English.vtt",
      "label": "English",
      "format": "vtt"
    }
  ]
}
```

## Error Response

```json
{
  "success": false,
  "error": "Missing id"
}
```

<Note>
  TMDB IDs for TV shows refer to the **series**, not individual episodes. Find them at [themoviedb.org](https://www.themoviedb.org).
</Note>

<Warning>
  The `headers` field on each source must be forwarded when fetching the stream directly. Use `/api/download/tv` to get pre-built download URLs and ffmpeg commands with headers already encoded.
</Warning>