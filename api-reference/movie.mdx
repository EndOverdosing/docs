---
title: Movie
description: Scrape streaming sources for a movie by TMDB ID
---

<Note>
  For enriched output with pre-built `download_url`, `ffmpeg_command`, and deeper deduplication across all providers in parallel, use [`/api/stream/movie`](/api-reference/stream-movie) instead.
</Note>

## Endpoint

```
GET /api/movie
```

Scrapes all providers for a movie and returns the raw source list. Sources are sorted by quality and filtered to English audio only. No deduplication beyond exact URL matches is applied at this layer.

---

## Parameters

<ParamField query="id" type="string" required>
  TMDB movie ID. Find it at [themoviedb.org](https://www.themoviedb.org) — it's the number in the URL, e.g. `themoviedb.org/movie/550` → `550`.
</ParamField>

---

## Example Request

<CodeGroup>
```bash cURL
curl https://vyla-api.pages.dev/api/movie?id=550
```

```js JavaScript
const res = await fetch("https://vyla-api.pages.dev/api/movie?id=550");
const data = await res.json();

console.log(data.sources); // array of stream sources
```

```python Python
import requests

data = requests.get("https://vyla-api.pages.dev/api/movie", params={"id": 550}).json()
print(data["sources"])
```
</CodeGroup>

---

## Response

<Tabs>
  <Tab title="Success">
    ```json
    {
      "success": true,
      "results_found": 8,
      "sources": [
        {
          "url": "https://vixsrc.to/playlist/170060?token=...&expires=...&h=1",
          "type": "hls",
          "quality": "1080p",
          "provider": "VixSrc",
          "audioTracks": [
            { "language": "eng", "label": "English" }
          ],
          "headers": {
            "Referer": "https://vixsrc.to",
            "Origin": "https://vixsrc.to"
          },
          "vlc_url": "https://vixsrc.to/playlist/170060?token=...&expires=...&h=1"
        },
        {
          "url": "https://bcdnxw.hakunaymatata.com/resource/abc123.mp4?sign=...&t=...",
          "type": "mp4",
          "quality": "720p",
          "provider": "02MovieDownloader",
          "audioTracks": [
            { "language": "eng", "label": "English" }
          ],
          "headers": {
            "Referer": "https://lok-lok.cc/",
            "Origin": "https://lok-lok.cc/"
          },
          "vlc_url": "https://bcdnxw.hakunaymatata.com/resource/abc123.mp4?sign=...&t=..."
        }
      ],
      "subtitles": [
        {
          "url": "https://vixsrc.to/playlist/170060?type=subtitle&rendition=eng&token=...&expires=...&edge=...",
          "label": "English",
          "format": "vtt"
        }
      ]
    }
    ```
  </Tab>
  <Tab title="Error">
    ```json
    {
      "success": false,
      "error": "Missing id"
    }
    ```
  </Tab>
</Tabs>

---

## Source Object Fields

<ResponseField name="url" type="string">
  Direct stream URL.
</ResponseField>

<ResponseField name="type" type="string">
  `hls`, `mp4`, or `mkv`
</ResponseField>

<ResponseField name="quality" type="string">
  e.g. `1080p`, `720p`, `HD`, `unknown`
</ResponseField>

<ResponseField name="provider" type="string">
  Which provider this source came from.
</ResponseField>

<ResponseField name="audioTracks" type="array">
  Language and label of available audio tracks.
</ResponseField>

<ResponseField name="headers" type="object">
  Required headers to forward when fetching this URL directly. **Must be included** or the CDN will return 403.
</ResponseField>

<ResponseField name="vlc_url" type="string">
  Same as `url` on this endpoint — not proxied. Use `/api/stream/movie` to get a proxied `vlc_url`.
</ResponseField>

---

<Warning>
  The `headers` field on each source **must be forwarded** when fetching the stream directly. Pass them as base64 JSON to `/api/proxy` via the `headers` param, or use `/api/stream/movie` which handles header encoding in `vlc_url` automatically.
</Warning>